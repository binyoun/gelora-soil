Self-hosted MediaPipe Tasks Vision model files. No CDN dependency at runtime.

- `hand_landmarker.task` — HandLandmarker task bundle, float16, from `mediapipe-models/hand_landmarker/hand_landmarker/float16/latest/`
- `selfie_segmenter.tflite` — ImageSegmenter model (raw tflite, not a `.task` bundle, per MediaPipe's ImageSegmenter API), from `mediapipe-models/image_segmenter/selfie_segmenter/float16/latest/`
